import type { Metadata } from "next";
import { CoupleFriendlyDelhiHotels } from "@/pages-legacy/destinations/DelhiSubLandings";

export const metadata: Metadata = {
  title: "Couple Friendly Hotels in Delhi — 100% Safe & Local ID Accepted | GetHotelStays",
  description: "Book verified couple friendly hotels in Delhi. Welcomes 18+ couples with local IDs. Safe, private & discreet stays. Pay 12% online, rest at check-in.",
};

export default function Page() {
  return <CoupleFriendlyDelhiHotels />;
}
